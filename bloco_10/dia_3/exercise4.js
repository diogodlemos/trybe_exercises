const uppercase = (str) => str.toUpperCase();

const returnFirstLetter = (str) => str[0];

const concatString = (str1, str2) => str1.concat(str2);

module.exports = {
  uppercase,
  returnFirstLetter,
  concatString
}
